volumes:
  n8n_storage:
  ollama_storage:
  open-webui:
  caddy-data:
  caddy-config:

x-n8n: &service-n8n
  image: n8nio/n8n:latest
  environment:
    - DB_TYPE=sqlite
    - N8N_DIAGNOSTICS_ENABLED=false
    - N8N_PERSONALIZATION_ENABLED=false
    - N8N_ENCRYPTION_KEY=${N8N_ENCRYPTION_KEY}
    - N8N_USER_MANAGEMENT_JWT_SECRET=${N8N_USER_MANAGEMENT_JWT_SECRET}
    - WEBHOOK_URL=http://localhost:5678

x-ollama: &service-ollama
  image: ollama/ollama:latest
  container_name: ollama
  restart: unless-stopped
  expose:
    - 11434/tcp
  environment:
    - OLLAMA_CONTEXT_LENGTH=4096
    - OLLAMA_FLASH_ATTENTION=1
    - OLLAMA_MAX_LOADED_MODELS=1
  volumes:
    - ollama_storage:/root/.ollama

x-init-ollama: &init-ollama
  image: ollama/ollama:latest
  container_name: ollama-pull-model
  volumes:
    - ollama_storage:/root/.ollama
  entrypoint: /bin/sh
  command:
    - "-c"
    - "sleep 5; OLLAMA_HOST=ollama:11434 ollama pull phi3:mini"

services:

  # ─── LLM ──────────────────────────────────────────────
  ollama-cpu:
    profiles: ["cpu"]
    <<: *service-ollama

  ollama-pull-llama-cpu:
    profiles: ["cpu"]
    <<: *init-ollama
    depends_on:
      - ollama-cpu

  # ─── WORKFLOW ─────────────────────────────────────────
  n8n:
    <<: *service-n8n
    container_name: n8n
    restart: unless-stopped
    ports:
      - "5678:5678"
    volumes:
      - n8n_storage:/home/node/.n8n
      - ./n8n/backup:/backup
      - ./shared:/data/shared
    environment:
      - N8N_BASIC_AUTH_ACTIVE=true
      - N8N_BASIC_AUTH_USER=${N8N_BASIC_AUTH_USER}
      - N8N_BASIC_AUTH_PASSWORD=${N8N_BASIC_AUTH_PASSWORD}

  # ─── CHAT UI ──────────────────────────────────────────
  open-webui:
    image: ghcr.io/open-webui/open-webui:main
    restart: unless-stopped
    container_name: open-webui
    ports:
      - "3000:8080"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - open-webui:/app/backend/data

  # ─── REVERSE PROXY ────────────────────────────────────
  # caddy:
  #   container_name: caddy
  #   image: caddy:2-alpine
  #   restart: unless-stopped
  #   ports:
  #     - "5678:5678"    # n8n
  #     - "3000:3000"    # open-webui
  #     - "8000:8000"    # fastapi (Phase 1)
  #   expose:
  #     - 2019/tcp
  #   volumes:
  #     - ./Caddyfile:/etc/caddy/Caddyfile:ro
  #     - ./caddy-addon:/etc/caddy/addons:ro
  #     - caddy-data:/data:rw
  #     - caddy-config:/config:rw
  #   environment:
  #     - N8N_HOSTNAME=${N8N_HOSTNAME:-":5678"}
  #     - WEBUI_HOSTNAME=${WEBUI_HOSTNAME:-":3000"}
  #     - LETSENCRYPT_EMAIL=${LETSENCRYPT_EMAIL:-internal}
  #   cap_drop:
  #     - ALL
  #   cap_add:
  #     - NET_BIND_SERVICE
  #   logging:
  #     driver: "json-file"
  #     options:
  #       max-size: "1m"
  #       max-file: "1"

  # ─── COMING SOON ──────────────────────────────────────
  # fastapi:         Phase 1 - backend API
  # postgres:        Phase 3 - SaaS database
  # searxng:         Optional - local web search
  # qdrant:          Optional - vector DB for RAG
  # langfuse:        Optional - LLM monitoring
